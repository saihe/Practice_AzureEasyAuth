version: '3.8'

services:
  frontend:
    container_name: frontend
    image: ghcr.io/saihe/practice_azureeasyauth/frontend:latest

    ports:
      - 80:80
  backend:
    container_name: backend
    image: ghcr.io/saihe/practice_azureeasyauth/backend:latest
    ports:
      - 8080:8080
